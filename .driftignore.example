# .driftignore — variables to exclude from drift detection
# (copy this to .driftignore and customize)
#
# System variables injected by the platform (not in config files)
PATH
JAVA_HOME
HOME
USER
PWD
SHELL

# Variables injected by CI/CD platform at runtime
CI
GITHUB_TOKEN
GITHUB_SHA
GITHUB_REF
