version: '3.8'
services:
  app:
    image: myapp:latest
    environment:
      - DATABASE_URL=postgres://db:5432/myapp
      - API_KEY=key-abc123
      - REDIS_HOST=redis
      # 11 variables missing from deployment config!
